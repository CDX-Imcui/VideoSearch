<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.springdemo.dao.AdminDao">
    <select id="searchAdmin" resultType="com.example.springdemo.entity.Admin">
        select * from admin
        <where>
            <!-- 这里的注释只能这样不能用# 这在sql语句内部 -->
            <!-- !和=之间不能有空格 -->
            <!-- role !="role_admin" -->
            <if test="params != null and params.id != null and params.id != null">
                id = #{params.id}
            </if>
            <if test="params != null and params.name != null and params.name != ''">
                and username like concat('%', #{params.name},'%')
            </if>
            <if test="params != null and params.major != null and params.major != ''">
                and major = #{params.major}
            </if>
            <if test="params != null and params.classname != null and params.classname != ''">
                and classname = #{params.classname}
            </if>
        </where>
    </select>

</mapper>